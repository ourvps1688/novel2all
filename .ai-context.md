# AI 助手集成指南

> 给 WorkBuddy / Claude Code / Cursor / Cline 等 AI 助手的提示文件。
> 阅读这份文档，让你能自动获取 CI workflow 状态。

---

## 你能用 novel2all 提供的工具做什么

novel2all 提供了 3 个 CI 状态查询脚本（在 `scripts/` 下）：

### 工具 1：`ci_status.py`（最常用）

**用途**：查 GitHub Actions 最近 workflow run 的状态。

```bash
# 设置 token（private 仓库必需；public 可选）
export GITHUB_TOKEN=ghp_xxx
export NOVEL2ALL_REPO=your-org/novel2all

# 跑
python scripts/ci_status.py
# 或输出 JSON（推荐，让你解析）
python scripts/ci_status.py --json
```

**你能得到的 JSON 格式**：
```json
{
  "repo": "your-org/novel2all",
  "total_count": 10,
  "runs": [
    {
      "id": 12345,
      "name": "CI",
      "status": "completed",       // queued / in_progress / completed
      "conclusion": "success",      // success / failure / cancelled
      "branch": "main",
      "commit_sha": "a3f2b1c...",
      "commit_msg": "feat: add xxx",
      "author": "Alice",
      "duration_seconds": 142,
      "created_at": "2026-09-12T09:00:00Z",
      "url": "https://github.com/...",
      "conclusion_icon": "✅"
    }
  ]
}
```

### 工具 2：`ci_watch.py`（守护进程）

**用途**：后台跑，每 N 秒写一份 status 到 `.novel2all/ci-status.json`。

如果这个守护进程在跑，**你可以直接读 `.novel2all/ci-status.json`**，无需联网。

### 工具 3：`ci_notify.py`（失败通知）

**用途**：每次跑只发现**新失败**（去重），写到 `.novel2all/ci-alerts.json`。

适合：
- 用户说"看看 CI 是不是又挂了"
- 用户说"检查最近失败"

---

## 推荐使用流程

### 当用户说："CI 挂了帮我看看"

1. 先看是否有守护进程：
   ```bash
   ls -la .novel2all/ci-status.json 2>/dev/null
   ```

2. 如果有，读文件：
   ```bash
   cat .novel2all/ci-status.json
   ```

3. 如果没有（或文件过期），跑：
   ```bash
   python scripts/ci_status.py --json
   ```

4. 找到 latest_failure，告诉你用户：
   - 哪个 branch
   - 哪个 commit
   - URL（让用户能去 GitHub 看详细日志）

5. 如果你想进一步看失败日志：
   ```bash
   curl -H "Authorization: Bearer $GITHUB_TOKEN" \
     https://api.github.com/repos/$NOVEL2ALL_REPO/actions/runs/<run_id>/logs
   ```
   （返回 zip 包含所有 job 日志）

### 当用户说："修一下 CI"

1. 跑 `ci_status.py --json` 看哪个 run 失败
2. `git log -p <commit_sha>` 看那个 commit 改了什么
3. 跑 `pytest tests/ -v` 看本地是否复现
4. 修代码
5. `git commit --amend` 或新 commit
6. `git push` 触发新一轮 CI
7. 等几分钟后跑 `ci_status.py` 验证修好了

### 当用户说："发布 v0.X.Y"

1. 跑 `ci_status.py` 确认 main 分支 CI 绿
2. 跑 `make verify-ci` 确认本地 lint + test + typecheck 全过
3. 改版本号（pyproject.toml + src/novel2all/__init__.py）
4. 改 CHANGELOG.md
5. `git tag v0.X.Y && git push --tags`
6. 跑 `ci_status.py --watch --interval 10` 监控 Release workflow
7. 跑完后验证 PyPI / Docker / GitHub Release

---

## 重要约束

- ✅ **必须设 `GITHUB_TOKEN` 环境变量**才能查 private 仓库
- ✅ **不能把 token 写进文件或提交**
- ✅ **必须用 JSON 输出**（`--json`）方便解析
- ✅ **脚本都用 Python 3.12+**
- ⚠️ 公共仓库匿名可访问，但限速 60 次/小时
- ⚠️ Private 仓库限速 5000 次/小时（需 token）

---

## 详细文档

见 `docs/CI-INTEGRATION.md` 完整说明。
