# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
.eggs/

# uv
.venv/
venv/
# 注意：uv.lock 必须提交，CI 的 setup-uv 缓存依赖它

# pytest
.pytest_cache/
.coverage
htmlcov/
.tox/

# mypy / ruff
.mypy_cache/
.ruff_cache/

# 环境
.env
.env.local
.env.*.local

# novel2all 项目元数据
.novel2all/cache/
.novel2all/memory.db
.novel2all/ci-status.json
.novel2all/ci-alerts.json
.novel2all/audit.db  # V1.0.1：runtime data，不入仓
.novel2all/sessions.db  # V1.0.1：runtime data，不入仓
.novel2all/adaptive_routing.db  # V1.0.1：runtime data，不入仓
.novel2all/traces.jsonl  # V1.0.1：runtime tracing 数据
.ci_watch.pid
.ci_watch.log

# CI scripts 临时文件
scripts/__pycache__/

# 测试日志（来自 PowerShell / Out-File）
.test*.log
.test*.py
*.test.log
FINAL-*.log
.ssh-test.log
.outbound.log
.perms.log
.api-info.log
.api-test.log
.api-info.py
.token-check.py
.token-check.log
.github-info.log
.github-info.py
.push.py
.push-result.log
.push-verbose.log
.push-trace.log
.remote.log
.add.log
.add2.log
.commit.log
.status.log
.status2.log

# AI 助手诊断文件（用前缀下划线命名，例如 _commit_msg.txt）
_*.txt
_*.py

# 用户项目
# 用户用 novel2all 创建的小说项目目录由用户自己管理 .gitignore

# V1.0.1：忽略 8-9 char lowercase alphanumeric 或带下划线的临时文件（如 "blat" artifacts）
# 这些是测试/工具脚本意外产生的占位文件（仅含 "blat" 等短内容）
# 不误伤合法文件（如 "000m5fp4.md" 8 字符但带扩展名）
/[a-z0-9]{8}
/[a-z0-9_]{8,9}

# 注意：之前用 [a-z0-9]{8,12} 类规则想忽略 git 自身的"blat"占位文件，
# 但这类规则会误伤合法文件（如 pipeline.py 的"pipeline"8 字符 + .py）。
# 改为：彻底不忽略，由用户手动 `git status --ignored` 看到后清理，
# 或者用 scripts/clean-gitblat.py 脚本。

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# 临时暂存区（_clean.py 移出根目录临时文件到这里，确认无问题后可删）
.junk/

# web-react 前端构建产物（2026-09-16 cleanup：避免 TS incremental build cache 与编译产物污染仓库）
# vite.config.ts 才是源；vite.config.js 是 tsx/typescript 编译产物
/web-react/vite.config.js
/web-react/vite.config.ts.timestamp-*.mjs
# TypeScript incremental build cache
/web-react/*.tsbuildinfo
# web-react 根目录下的临时测试/build 输出（_*.txt 已在根规则覆盖，但 web-react/ 下需要独立路径）
/web-react/*.txt
